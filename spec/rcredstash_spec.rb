require 'spec_helper'

describe Rcredstash do
  it 'has a version number' do
    expect(Rcredstash::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
